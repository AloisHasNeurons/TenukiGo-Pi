# --- Dépendances de l'application (du fichier original) ---
tensorflow==2.17.0
Flask==3.1.0
numpy<2
opencv-contrib-python==4.8.1.78
opencv-python==4.10.0.84
opencv-python-headless==4.10.0.84
scikit-learn==1.6.0
sente==0.4.2
sgf==0.5
ultralytics==8.0.206

# --- Ajouts pour la stabilité et la conversion (MAINTENANT VERROUILLÉS) ---

# 1. Pour éviter l'erreur de "pickle" de PyTorch 2.6+
torch==2.5.0
torchvision==0.20.0

# 2. Outils de conversion (versions plus anciennes compatibles)
onnx-tf==1.10.0
onnx==1.10.2

# 3. Pour envoyer le SGF au serveur FastAPI
websockets==15.0.1